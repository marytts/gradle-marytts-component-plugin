plugins {
    id 'de.dfki.mary.component'
}

ext {
    componentName = 'MyComponent'
    componentPackageName = 'mypackage'
}

apply from: 'test-tasks.gradle'

task testConfig {
    doLast {
        assert marytts.component.config == [:]
    }
}
