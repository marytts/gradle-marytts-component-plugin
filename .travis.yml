branches:
  only:
    - master

language: groovy
jdk:
  - openjdk8
  - openjdk10
  - openjdk11

script: ./gradlew check --info

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
